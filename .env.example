# Claude to Trilium Sync Configuration
# Copy this to .env and fill in your values

# Required: Trilium ETAPI token
# Get from: Trilium -> Options (top right gear) -> ETAPI -> Create new token
TRILIUM_TOKEN=your_trilium_etapi_token_here

# Required: Claude session cookie
# Get from: claude.ai -> F12 DevTools -> Application tab -> Cookies -> sessionKey
# Note: This expires periodically, you'll need to update it
CLAUDE_SESSION_KEY=sk-ant-sid01-xxxxxxxxxxxxx

# Trilium server URL
# If running Trilium in Docker on same network, use container name
# If Trilium is external, use full URL like http://192.168.1.100:8080
TRILIUM_URL=http://trilium:8080

# Parent note configuration
# Option 1: Specify an existing note ID to import under
# Get the note ID from Trilium (right-click note -> Note Info, or from URL)
# PARENT_NOTE_ID=aBcDeFgHiJkL

# Option 2: Let it auto-create a parent note with this title (default: "Claude Chats")
PARENT_NOTE_TITLE=Claude Chats

# Sync interval in seconds
# 3600 = 1 hour (default)
# 1800 = 30 minutes
# 0 = run once and exit (for cron-based scheduling)
SYNC_INTERVAL=3600

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# FlareSolverr URL (required for Cloudflare bypass)
# If running from included docker-compose, default works
# If using external FlareSolverr, specify full URL with /v1 path
FLARESOLVERR_URL=http://flaresolverr:8191/v1

# Pushover notifications (optional)
# Get your user key from: https://pushover.net/
# Create an application at: https://pushover.net/apps/build
PUSHOVER_USER_KEY=
PUSHOVER_API_TOKEN=
# Optional: Send to specific device only
PUSHOVER_DEVICE=
